# E<番号>: <名前>

- 状態: 依頼中（未依頼 → 依頼中 → 実施済み → 反映済み）
- 依頼日:
- 実施日:
- 実施者: （GitHub のユーザー名。本名やメールアドレスは書かない）

## 目的

何を確かめるか。結果で何が決まるか（決定 ID・設計書の節）。

## 必要なもの

- 機材:
- ソフトウェア:
- 所要時間の目安:

## 安全上の注意

- 止め方を必ず書く。確実なのは電源を切ること。停止トークン `d` を使うなら、その送り方も書く。

## 手順

コマンドは、リポジトリのルート（`Cargo.toml` がある場所）で実行する前提で書く。
生の出力は `experimentals/raw/` に保存する（コミットされない）。

1. （**未実行**）`cargo run --release -p walk-experiments --bin eXX-<名前> -- <PORT> <トークン>` のように、確かめていないものには印を付ける
2.

## 記録すること

- [ ] 環境（ファームのバージョン、接続手段、Mac の OS・CPU、`rustc --version`）
- [ ]

## 判定基準

結果の場合分けに漏れと重なりが無いように書く。最後の行は必ず「判定できない」にする。

| 観察 | 結果 | 設計への影響 |
|---|---|---|
|  |  |  |
| （全体） | 手順どおりに進まなかった・判定できない | 再依頼する。それまで、この実験を待つ設計（決定 ID）は保留のまま |

## 結果（実施者が記入）

貼る前に [public-repo.md](../docs/rules/public-repo.md) に従って伏せる（IP → `<IP>`、ポート名 → `<PORT>` など）。
コミットの前に `git diff --staged` を読み、`scripts/check-public.sh --staged` を通す。
API キーを使った実験では、キーが含まれていないことを [public-repo.md](../docs/rules/public-repo.md) 3節の方法で確かめる。

### 環境

- ファームウェアのバージョン:
- 接続手段:
- Mac の OS・CPU:
- `rustc --version`:

### 出力・観察

```
（実行したコマンドと出力。伏せた上で貼る）
```

### 判定

### 気づいたこと
