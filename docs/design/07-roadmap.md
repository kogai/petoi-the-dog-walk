# 07. ロードマップ（設計案）

| フェーズ | 内容 | 完了条件 | 待つ実験 |
|---|---|---|---|
| 0 | ハーネス: テスト・静的検査・CI・レビュー体制・ドキュメント | `scripts/check.sh` と CI が通る。rules が揃う | なし |
| 1a | `domain`・`arbiter`・`skills`・`profiles` を Fake だけで実装 | 単体・プロパティテストが通る | なし |
| 1b | `reasoning`（記録済み応答で）と `loop` を Fake で結合 | Fake 一式でループが決定的に回る | E05（英訳の要否） |
| 1c | 本物の `transport` をつなぐ | 実機で指示どおりに座る・歩く・止まる | E00 |
| 2 | 実験結果を反映し、`sensing` を足す | 状況（`situation`）に実センサーが入る | E01, E02, E04, E06, E07 |
| 3 | 反射層（ハエ脳）を足す | 反射層の有無で振る舞いが変わる | E08, D-06 |

- フェーズ 1a は実験を待たずに進められる。
- 実機を動かすテスト（1c 以降）は人が実行し、結果を `experimentals/` に記録する。
