# 決定事項・未決定事項

決まったら「状態」を「決定」に変え、「決定内容・理由」と日付を書く。決定を覆すときは行を消さず、新しい ID を足す。
状態（未決定・決定など）と決定内容は、この表だけに書く。候補の比較など論点の詳細は、表の「決め方」にある章に書く。

| ID | 論点 | 状態 | 決め方 | 決定内容・理由 |
|---|---|---|---|---|
| D-01 | 安全上の例外（転倒しそうなとき等）を優先順位の外に置くか | 未決定 | E01 で転倒を検知できるか次第。置く場合は `arbitrate` の `Rule::Safety` として最初に判定し、テストで固定する | |
| D-02 | 反射で反応させる対象（接近、持ち上げ、転倒、音など） | 未決定 | E01・E02 の結果で選べる範囲が決まる | |
| D-03 | 調停規則3「上の2つが無い場合」の定義 | 未決定 | [05](05-arbitration.md) 4節の候補から、E08 で決める | |
| D-04 | 日本語の性格文を英訳して Jev に渡すか | 未決定 | E05 で決める | |
| D-05 | 性格・スキルのファイル形式 | 仮決定 (2026-09-25) | フェーズ 1a で見直す | `personality.md`（自由文）＋ `skills.toml`（名前・説明・トークン列）。理由: 性格は非エンジニアが書く自由文なので Markdown、スキルはコードがトークン列として読むので構造化形式 |
| D-06 | ハエ脳の実行方法（Node の子プロセス / Rust へ移植 / ブラウザ） | 未決定 | [04](04-reflex-layer.md) 5節。フェーズ3の前に、公式の JavaScript が乱数の種を指定できるかも確かめて決める | |
| D-07 | jump_away / punch / kick / block の Bittle 側の対応 | 未決定 | フェーズ3で決める | |
| D-08 | 公式 Python API（PetoiRobot）を使うか | 決定 (2026-09-25) | — | 使わない。理由: 本体を Rust にする（D-09）。トークンの送信は `serialport` クレートと HTTP で足りる（F-B4、F-B5） |
| D-09 | 実装言語 | 決定 (2026-09-25) | 人が選んだ | Rust。理由: 人の方針（静的型検査があり、動的な逃げ道が少ない言語。Python は抑える）。enum と網羅的な match、`Result`、既定で不変の値で、判断のロジックを型で固められる。`#![no_std]` のクレートで、純粋な部分に I/O・時計が混ざらないことをコンパイラに守らせられる。Mac のシリアルは `serialport` クレートで扱える。ハエ脳（約1.2万ニューロン、100ms 周期）を移植しても性能は足りる見込み（未確認）。検討して採らなかった案: Python（動的）、TypeScript（型に `any` などの逃げ道が多く、実行時は動的）、Haskell（シリアルのライブラリが手薄）、OCaml（エコシステムが小さい）、F#（.NET 由来の null・例外が境界から入る） |
| D-10 | Jev の呼び方 | 仮決定 (2026-09-25) | E05 で確かめる | HTTPS の API を直接呼ぶ（cookbook が案内する SDK は Python 用なので使わない。F-J7）。理由と注意: 呼び方の根拠 F-J9 は第三者のコードから読んだ二次情報。E05 で実際に呼べて、公式ドキュメントでも確かめられたら「決定」にする。呼べなかったら開き直す |
| D-11 | 規則1（指示）と規則2（性格）を、Jev への質問でどう分けるか | 未決定 | [05](05-arbitration.md) 4節。E05 で決める | |
| D-12 | 開発環境の管理 | 決定 (2026-09-25) | オーナーの指示 | [mise](https://mise.jdx.dev/) で管理する。ツールの版は `mise.toml` だけに書く（`rust-toolchain.toml` は使わない）。CI も mise で同じ版を入れ、`mise run check` を実行する |
